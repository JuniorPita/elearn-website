// Reusable CSS classes
.section {
    padding:
    &__title {
        font-size:
        text-align: center;
        margin-bottom:
        
        &-gradient {
            background:
            color: transparent;
            -webkit-background-clip: text;
            background-clip: text;
        }
    }
}

// Layout
.main {
    overflow: hidden;
}

.container {
    max-width:
    margin-left:
    margin-right:
}

.grid {
    display: grid;
}