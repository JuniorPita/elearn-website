// Buttons
.button {
    display: inline-block;
    background-color:
    color:
    padding:
    border-radius:
    transition:

    &:hover {
        background-color:
    }

    &__icon {
        font-size:
    }
    &-flex {
        display: inline-flex;
        align-items: center;
        column-gap:
    }
}